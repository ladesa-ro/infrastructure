---
- name: Reiniciar o journald
  ansible.builtin.systemd:
    name: systemd-journald
    state: restarted

- name: Aplicar os parâmetros de kernel
  ansible.builtin.command:
    cmd: sysctl --system
  changed_when: true
