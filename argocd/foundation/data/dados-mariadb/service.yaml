---
apiVersion: v1
kind: Service
metadata:
  name: db-mariadb
  namespace: dados
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: mariadb
    k8s.mariadb.com/role: primary
  ports:
    - port: 3306
      targetPort: 3306
